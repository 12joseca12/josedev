import type { SobreMiTechKey } from "@/lib/types";

/** Tecnologías destacadas en la ficha 3catinfo (coincide con el diseño). */
export const CATINFO_SHOWCASE_TECH: readonly SobreMiTechKey[] = [
  "reactNative",
  "firebase",
  "nodejs",
  "expo",
  "supabase",
  "pushNotifications",
] as const;
